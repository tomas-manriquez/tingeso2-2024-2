spring:
  application:
    name: m6-calculo-costos-totales
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
  config:
    import: configserver:http://localhost:8888
  datasource:
    url: jdbc:postgresql://localhost:5432/tingeso-users-db
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update